import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css"; // 이거 있으면 남기고, 없으면 주석처리

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
